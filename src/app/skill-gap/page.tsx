import { requireUserId } from "@/lib/auth"; import { getStudentSkillGap } from "@/lib/student-skill-gap"; import { CareerGoalSelector } from "@/components/career-goal-selector"; import { StudentSkillsManager } from "@/components/student-skills-manager"; import { DashboardShell } from "@/components/layout/dashboard-shell";
export default async function SkillGapPage(){const id=await requireUserId();const gap=await getStudentSkillGap(id);return <DashboardShell title="Skill Gap" description="Your readiness is calculated deterministically from the role requirements and proficiency levels you save.">{!gap?<><div className="status-empty mt-8"><b className="block text-ink">Start your skill-gap analysis</b><p className="mt-2">Choose a target role and add your current skills to see a readiness score.</p></div><CareerGoalSelector/><StudentSkillsManager/></>:<><section className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-5">{[['Readiness',`${gap.readinessScore}%`],['Required',gap.summary.totalRequiredSkills],['Mastered',gap.summary.masteredSkills],['Improving',gap.summary.needsImprovement],['Missing',gap.summary.missingSkills]].map(([label,value])=><article className="surface p-5" key={String(label)}><p className="text-sm text-stone-600">{label}</p><p className="mt-2 text-3xl font-bold text-moss">{value}</p></article>)}</section><section className="surface mt-6 p-5 sm:p-6"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="eyebrow">Target role</p><h2 className="mt-1 text-xl font-bold">{gap.jobRole.title}</h2></div><div className="h-2 w-full max-w-sm overflow-hidden rounded-full bg-[#e5eee1]"><div className="h-full bg-brand" style={{width:`${gap.readinessScore}%`}}/></div></div><div className="mt-6 grid gap-3 md:grid-cols-2">{gap.skills.map(skill=><article className="rounded-xl border border-stone-200 p-4" key={skill.skillId}><div className="flex items-start justify-between gap-3"><b>{skill.skillName}</b><span className={`rounded-full px-2 py-1 text-xs font-bold ${skill.status==='mastered'?'bg-green-100 text-moss':skill.status==='missing'?'bg-rose-100 text-[#914653]':'bg-amber-100 text-amber-800'}`}>{skill.status==='needs_improvement'?'Improving':skill.status}</span></div><p className="mt-2 text-sm text-stone-600">Current {skill.studentLevel}/5 · Required {skill.requiredLevel}/5 · Importance {skill.importance}/5</p><div className="mt-3 h-2 overflow-hidden rounded-full bg-stone-100"><div className="h-full bg-brand" style={{width:`${Math.min(100,(skill.studentLevel/skill.requiredLevel)*100)}%`}}/></div></article>)}</div></section><CareerGoalSelector/><StudentSkillsManager/></>}</DashboardShell>}
